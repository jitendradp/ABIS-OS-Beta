OpenShift
==============================

Layer:
------------------------------
  0) Volumes/Partitions für
     * .. Datawarehouse (Stern/Snowflake-Schema)
     * .. Große Tabellen: Group, Entry, Tag

  1) Postgres
     * Write-master/read-slaves

  2) Prisma (JVM)
     * Kann in Cluster-Konfiguration betrieben werden (TBD)

  3) Yoga (NodeJS)				                                 <->		   3.5) Redis
     * Stateless, kann dynamisch horzontal skaliert werden	         * PubSub
     * Config über ENV-Vars					                                 * Cache
     * Authentication via Cookie

  4) Frontend (Angular)
     * Liegt in CDN
     * Verbindet sich per WS und HTTP mit Yoga


Brauchen:
------------------------------
* Load-Balancer
* Monitoring
* CI-Pipeline von Github. Src -> Docker-Image -> Kubernetes/OpenShift
