export class GroupQueries2 {
    static findRooms(csrfToken: string, bearerToken: string, searchText?: string) {
        return undefined;
    }

    static findMemberships(csrfToken: string, bearerToken: string, roomId: string, searchText?: string) {
        return undefined;
    }

    static getEntries(csrfToken: string, bearerToken: string, groupId: string, from?: Date, to?: Date) {
        return undefined;
    }
}
