import {GroupType} from "../../../generated";

export class AgentQueries {

    static myStashes(csrfToken: string, bearerToken: string) {
        return undefined;
    }

    static myChannels(csrfToken: string, bearerToken: string) {
        return undefined;
    }

    static myRooms(csrfToken: string, bearerToken: string) {
        return undefined;
    }

    static myMemberships(csrfToken: string, bearerToken: string, groupType?: GroupType, isPublic?: boolean) {
        return undefined;
    }
}
